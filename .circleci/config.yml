version: 2.1
orbs:
  cloudrun: circleci/gcp-cloud-run@1.0.2
jobs:
  build_and_deploy_on_cloud_run:
    machine:
      image: ubuntu-2004:current
      docker_layer_caching: true
    steps:
      - attach_workspace:
          at: .
      - checkout
      - cloudrun/init
      # - cloudrun/build:
      #     tag: europe-north1-docker.pkg.dev/audiosetrecorder-2022/crystal/crystal/edy-latest
      - cloudrun/deploy:
          image: europe-north1-docker.pkg.dev/audiosetrecorder-2022/crystal/crystal/edy-latest
          platform: "managed"
          region: "europe-north1"
          service-name: "edy"
          unauthenticated: true

  decode_key_to_json:
    machine:
      image: ubuntu-2004:current
      docker_layer_caching: true
    steps:
      - run: echo ${GCLOUD_SERVICE_KEY} | base64 --decode --ignore-garbage > gcloud-service-key.json
      - store_artifacts:
          path: gcloud-service-key.json
      - persist_to_workspace:
          root: .
          paths:
            - "gcloud-service-key.json"

  export_var:
    machine:
      image: ubuntu-2004:current
      docker_layer_caching: true
    steps:
      - run: export 
  
workflows:
  launch:
    jobs:
      - build_and_deploy_on_cloud_run